# Pollinate strategy docs

**Gold source, posted by Colin 2026-08-13T15:45Z.** These four files replace the previous `POLLINATE_{STRATEGY,PRD,DELIVERY_SLICES}.md` set, which Colin instructed us to delete. The old files carried agent addenda layered on top of an earlier direction; these were Colin's own cleaned-up versions and superseded them entirely.

**Superseding ruling, 2026-08-17 (Colin, "repo wins"; reconciliation landed by Fizz in `cd04e40`, `882c93f`).** The instruction above — no re-added addenda, raise a question in the channel instead — is retired. `docs/strategy/` is now the single canonical home for these four files (and `PLANS/Pollinate_*.md` in the workspace are pointer stubs, not specs); the workspace amendment-and-ruling workflow the old instruction was written to keep out is now how this directory gets updated. All four files carry substantial 2026-08-17 amendments and are current. Raise a question in the channel for anything that looks contradictory, same as before — the change is that landing a ratified amendment here no longer needs a separate ask.

**Ruling propagation (standing rule, 2026-08-17; owner: Lumen — encoding rulings is Lumen's step).** When a ruling changes vocabulary (seal vs. send, subject vs. recipient) **or cuts scope (amended 2026-08-19: a deferral retires *claims*, not just nouns — the 8b.8 → Slice 1.1 cut got no sweep under the vocabulary-only trigger, and "recipient can react and reply" sat in three rows for two days, fixed `984c22c`; for a scope cut, the retired token is the cut feature's verbs)**, the encoding pass sweeps the retired vocabulary across every file in this directory before the ruling is called encoded. This is a manual ritual, not a gate: a line-oriented grep cannot see a markdown table's free/paid axis when it sits in the header row one line above (the PRD's recipient row was found by eye, not by any sweep), so a green sweep never licenses the claim "the canonical set agrees with itself."

The ritual, in order:

1. **Re-read every cited row by eye.** The eye-read is the acceptance check; the sweeps below are regression nets for rows nobody cited.
2. **Sweep the retired token, not the replacement.** Searching the new noun can only re-confirm edits already made — an unrenamed row contains none of the new vocabulary. Only the retired token can return a row not yet touched.
3. **Hand-classify every hit.** The retired noun keeps legitimate uses (metric labels, schema-state prose like `packaged`→`sent`, open media-scope questions). Publish the sweep as one pipeline with both yields labelled — the bare grep and the filtered count are different numbers (2026-08-17 package→send pass at `e4269e9`: bare 81, filtered 9, all 9 classified legitimate):

   ```sh
   git grep -nEi 'package' -- docs/strategy/ ':!docs/strategy/README.md' \
     | grep -Ei 'upgrade|free tier|paid|plus|unlimited|limit|tier|more |per year|gate'
   ```

   (Substitute the retired token and its tier/gate context words for the ruling at hand.) **The instrument does not count its own documentation**: this README is excluded, because every ruling recorded here adds its retired token to the corpus — the sweep's example command literally contains the string it searches for. Without the exclusion the bare yield drifts upward with each entry (83 at `fda5f79` against the 81 documented above), and a future entry that writes a retired token beside a context word would inflate the *filtered* count, the number people act on. The exclusion reassigns coverage rather than dropping it: this README's own descriptive bullets (the Slice 1 project list, the deferral notes, the ledger branch's status) are covered by step 1's eye-read, not the sweep — nothing gets excluded without a named place its coverage goes. **Amended 2026-08-30 (roles clause; wording @Lumen, encoded @Vector):** this file plays **two** roles — the sweep's documentation *and* the canonical index — and the exclusion protects the first while blinding the second. The eye-read's coverage of this README therefore includes its **ruling entries**, not only its descriptive bullets: an entry that *asserts* a since-retired ruling is invisible to the sweep by construction (2026-08-30: `README:25`'s metering sentence sat retired-but-asserted while `COPY-13` was owed). **When a ruling retires a token, step 1's eye-read opens this README first.**
4. **The verdict line reads "N hits, all classified legitimate" — never "zero hits."** Zero means the sweep is broken, not that the docs are clean.

**V2 adoption, 2026-08-25 (Colin, #CEO action items, event `d99dd08daeeb5fef9c27d071d87cf239028fb85441a565bb086c6c5b306db5d5`; encoded by Lumen same day).** `POLLINATE_V2_SPEC.md` and `POLLINATE_V2_ASSIGNMENTS.md` join this directory as the governing V2 direction. They supersede the Slice 2 wallet direction (PRD §5.6, Strategy §6, the Slice 2 rows of Delivery Slices) and the 2026-08-19 "1 hive, lifetime" revenue ruling — the ruled model is $39.99/yr annual-only, paywall at *delivery* (first delivery free forever), creation and writing unmetered. Slice 1 ships first, unchanged. Owed sweeps under the ritual above: **COPY-11** (retired tokens `$2.99`, `/mo`, `monthly`, `1 hive`, `lifetime` — swept 2026-08-25, this branch) and **COPY-5** (retired token `package` / `seal the hive` → volume vocabulary — after ENG-46 lands). Owner of both: Lumen. **[Amended 2026-08-30 (MVP-Comb pivot — see the Comb Rotation entry below; wording @Lumen, encoded @Vector): the revenue sentence above is retired — the $39.99/yr annual-only delivery-paywall model is superseded by the **per-user comb subscription**, price unruled until `C1` and `C5` (`POLLINATE_COMB_ROTATION.md` §4, `O4`). The "Slice 1 ships first" sequencing is likewise superseded — retired at `POLLINATE_COMB_ROTATION.md` **§9** ("there is one release: MVP-Comb"), closed as **`O5`** by `a11aa144…`; **not** §1A, which scopes MVP-Comb rather than retiring the sequencing. The entry stays as the record of the 08-25 adoption; the admissions themselves are unchanged.]**

**Metering principle, 2026-08-25 (Colin, event `5139a250…`; carried since the Amendment 2026-08-25 in Colin's own words as V2 spec §17.5.2a/b — Lumen's interim §17.5.6 encoding retired as a duplicate, all citations re-pointed).** The tier splits COPY-11 flagged are removed: review cadence ungated, friend connections uncapped, seed counts removed (abuse = rate limit ~5/week, never a price). **Delivery is the only meter** — "meter the artifact, never the practice, never the graph." Retired tokens swept same commit: `1 friend`, `1 seed` / `1 active seed`, `yearly only`, `yearly for free` / `monthly for Plus`. One *derived* extension is flagged in-place (PRD §6 paid table): the seed-type unlocks (Bloom When / Surprise / Reciprocal) moved to free by the principle, not by the ruling's letter. **[Amended 2026-08-30 (Colin's MVP-Comb pivot, #Strategy `e8cec2c0…`, scope `cf648e7f…`; encoded in `POLLINATE_COMB_ROTATION.md` — see its admission entry below; wording @Lumen, encoded @Vector): "Delivery is the only meter" is **retired** — `ENG-76` is cancelled, the $39.99/yr delivery-metered paywall is out, and Phase 4 monetization is out of MVP-Comb scope with **the price deliberately unruled until `C1` and `C5` return** (`POLLINATE_COMB_ROTATION.md` §4, `O4`). What survives is the narrower half: *meter the artifact, never the practice, never the graph* — kept as a **constraint on any future meter, not a claim that one exists**. This entry is **superseded on the metering sentence and current on the tier-split removals** (cadence ungated, connections uncapped, seed counts removed — all still live under the comb model); `POLLINATE_COMB_ROTATION.md` governs.]**

**Navigation ruling, 2026-08-26 (Colin, #CEO action items, event `4e4d6d3ded7cc472c35b844362d46770d54476e9b20115b67f999e60c5ae26e6`; encoded by Lumen same day).** `POLLINATE_V2_NAVIGATION.md` and `DESIGN_BRIEF_V2_NAVIGATION.md` join this directory; the 2026-08-26 amendment is appended to `Pollinate_The_Ruling.md`. Retired token: the *build/keep the Wallet shell tab* instruction (`Wallet tab`, `Wallet (shell`, `wallet is a shell`). Sweep run at encoding: bare 20 hits, all classified legitimate — 11 supersession annotations written this pass (Slices 1/§39/10.1/11.8/scope-table/15.6, PRD tab-diagram/tab-row/§Wallet-Tab, Strategy tab-line/launch-checklist), 6 descriptive-of-the-deletion (spec §5.2(b), nav doc, ENG-65/81 rows, Ruling amendment), 3 in `Pollinate_The_Ruling.md`'s 2026-08-17 body (:117, :237, :246) left byte-untouched under append-only amendment discipline and superseded by the amendment's own first line. Slice 3+ wallet *infrastructure* deferrals (money plumbing, Projects 3/4/5) are unaffected — the ruling deletes the tab, not the money constraints.

**Comb Rotation Ruling, 2026-08-30 (Colin, #Strategy thread `e8cec2c0…`; final model at event `d662661b…`).** `POLLINATE_COMB_ROTATION.md` joins this directory as the governing ruling for product focus, tiering and build sequence. **The comb rotation (V2 §18.2) is promoted from Cycle 11–12 to the hero; private hives become the personal mode; Pollinate is free for everything except the comb.** The paid line is a **per-user subscription** unlocking more than one comb to write in and more than 5 members in a comb you run — **and the price is deliberately unruled** until conditions C1 and C5 return data. **Receiving is never metered:** membership means writing rights, and `comb_rotations.subject_profile_id` references `profiles`, not `comb_members`, so a comb may write for anyone. **MVP scope ruled the same night (`cf648e7f…`): Phases 0–2 of that document's §8.6 are the MVP — "MVP-Comb" — to be built to completion now; Phase 4 (IAP, paywall, cap enforcement) waits on Phase 3's numbers. This partly reverses the 2026-08-26 "analytics/legalcopy is mvp2" amendment in `Pollinate_Delivery_Slices.md` (`OPS-8` is in Phase 0) and retires the "Slice 1 ships first, then combs" sequencing — see §1A, which resolves all three collisions rather than leaving them to be discovered.** Also ruled: no physical printing ever (`482eee85…`), the Wallet tab stays deleted (re-examined against the daily-nectar premise), and short note + nectar is unscoped from the reveal into 19a simulated. Amendments appended to `Pollinate_The_Ruling.md`, `Pollinate_Delivery_Slices.md` and `Pollinate_PRD.md`; annotation blocks written in place (bytes untouched) in `POLLINATE_V2_SPEC.md` §0/§5.2(d)/§17.5/§17.5.1/§17.5.2a/§17.5.2b/§17.5.3/§17.5.4/§18.2/Apple-3.1.1, `Pollinate_Strategy.md` §1/§4/§5/§7/§9, and `POLLINATE_V2_ASSIGNMENTS.md` §5/§8/§11 (+ new §5A). Retired tokens: `$39.99`, `annual only`/`annual-only`, `$79`, `$5.99`, `metered at delivery`, `delivery is the only meter`, `first delivery free`, `organizer pays`. **Sweep owed: `COPY-13`, owner Lumen — un-run at the time this was written.** Encoding authored by Vector at Colin's direct request; **ratification remains Lumen's step.** Note that §17.5.1's annual-over-monthly finding was briefly annotated as superseded and then **upheld** the same day — read that annotation, not the first draft of it. Rulings O1 and O2 are closed; **O3** (projected-returns contradiction), **O4** (the price), **O5**/**O6**/**O7** were closed the same night (`a11aa144…`: one release; `ENG-89`+`ENG-78` in MVP-Comb at Phase 2.7; EAS internal distribution via `OPS-10`, TestFlight stays MVP2), and the §1A definition-of-done is ratified as the acceptance test. **O3** (projected-returns contradiction) and **O4** (the price) stay open, collected in the new doc's §10. Positions considered and rejected are recorded in its §11 so they are not re-proposed.

- [`POLLINATE_COMB_ROTATION.md`](./POLLINATE_COMB_ROTATION.md) — **the operative product-focus, tiering and sequencing ruling** (Colin, 2026-08-30). §3 the tier contract; §4 why the price is blank; §5.3 why there is still no Wallet tab; §6 the five conditions that decide the business and what each failure signature means; §7 design handoff; §8 engineering handoff, including **§8.5 the sequencing trap** (build the caps, do not enforce them while measuring) and **§8.6 the phased build sequence with owners**; §10 open rulings; §11 positions rejected.
- [`Pollinate_The_Ruling.md`](./Pollinate_The_Ruling.md) — **read this first.** Colin's answer to the scope/alignment memo: the journal is the foundation (not legacy), the tab bar is `Today | Hive | Garden` (amended 2026-08-26 — the Wallet tab is deleted, the comb is the wallet; the original 2026-08-17 body still reads four tabs, superseded by the appended amendment), private hives live in Today, Wrapped moves to Garden, money is deferred to Slice 2, and the two P0 engineering fixes (run the migrations; move journal storage to Supabase).
- [`Pollinate_Strategy.md`](./Pollinate_Strategy.md) — positioning ("a journal that becomes social"), audience, cold-start via private hives, business model, moats.
- [`Pollinate_PRD.md`](./Pollinate_PRD.md) — product requirements. §5.1 Private Hives is the hero feature; §7 is the data-architecture rule (if losing the phone destroys it, it belongs in Supabase).
- [`Pollinate_Delivery_Slices.md`](./Pollinate_Delivery_Slices.md) — the source for work assignments. Slice 1 is Projects 1, 2, 6, 7, 8, **8b (new — Private Hives)**, 9, 10, 11. Projects 3/4/5 (wallet, funding, onramp) are deferred past Slice 2.
- [`POLLINATE_LEDGER_DESIGN.md`](./POLLINATE_LEDGER_DESIGN.md) — double-entry ledger design. No longer reference-only as of V2: ENG-62 lands `bumble/nectar-ledger-schema` with `rails_mode=simulated` for 19a (still no live-money risk; real rails are Cycle 13+, gated).
- [`POLLINATE_V2_SPEC.md`](./POLLINATE_V2_SPEC.md) — **the governing V2 spec** (adopted 2026-08-25): positioning amendment, Project 16 prompt engine, Project 17 volumes & delivery, Project 18 collective hives & combs, Project 19 nectar zaps, Project 20 analytics, §17.5 revenue model.
- [`POLLINATE_V2_ASSIGNMENTS.md`](./POLLINATE_V2_ASSIGNMENTS.md) — V2 work breakdown: issue IDs, owners, sequencing, critical path, gates. The source for V2 work assignments.
- [`POLLINATE_V2_NAVIGATION.md`](./POLLINATE_V2_NAVIGATION.md) — **the operative navigation ruling** (Colin, 2026-08-26): three-tab bar `Today | Hive | Garden`, Wallet tab deleted, account door top-right, where money lives without a tab. Companion to the 2026-08-26 amendment in `Pollinate_The_Ruling.md`.
- [`DESIGN_BRIEF_V2_NAVIGATION.md`](./DESIGN_BRIEF_V2_NAVIGATION.md) — the design brief for the navigation ruling: Part A (DES-27, three-tab capsule + door) and Part B (DES-28, zap surfaces; Surface 1 inherits DES-24).

`README.md` and `PROJECT_STRUCTURE.md` at repo root still describe the pre-pivot app; see `pixel/pollinate-rebrand-inventory` for the pending rewrite of those.
